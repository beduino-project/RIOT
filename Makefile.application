MODULE = $(APPLICATION)

DIRS += $(RIOTCPU)/$(CPU) $(RIOTBOARD)/$(BOARD)
DIRS += $(RIOTBASE)/core $(RIOTBASE)/drivers $(RIOTBASE)/sys

ifeq (1,$(RUST_BUILD))
DIRS += $(RIOTRUST)/cpu/$(CPU)
DIRS += $(RIOTRUST)/core $(RIOTRUST)/drivers $(RIOTRUST)/sys
endif

include $(RIOTBASE)/Makefile.base
